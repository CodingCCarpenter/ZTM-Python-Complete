# does make code less readable, so be careful
# Lambda expressions
my_list = [5,4,3]

# write a lambda expression that can return the squares of our list
print(list(map(lambda item: item ** 2, my_list)))